package com.xckrt.studentplanner.ui.theme

import androidx.compose.ui.graphics.Color

// --- Цвета для Темной темы ---
val DarkPrimary = Color(0xFF80D4CB)    // Мягкий бирюзовый
val DarkBackground = Color(0xFF191C1C) // Глубокий графит (не чисто черный)
val DarkSurface = Color(0xFF222626)    // Цвет карточек (чуть светлее фона)
val DarkOnSurface = Color(0xFFE1E3E3)  // Светло-серый текст

// --- Цвета для Светлой темы ---
val LightPrimary = Color(0xFF006A60)   // Насыщенный тил
val LightBackground = Color(0xFFFBFDFD) // Очень мягкий серый (почти белый)
val LightSurface = Color(0xFFFFFFFF)    // Чисто белые карточки
val LightOnSurface = Color(0xFF191C1C)  // Почти черный текст для контраста

// --- Семантические цвета (для недель и статусов) ---
val ColorNumerator = Color(0xFFE2A000)   // Приглушенный золотой
val ColorDenominator = Color(0xFFC85A91) // Пыльная роза
val ColorLessonTime = Color(0xFF4DB6AC)  // Время пары